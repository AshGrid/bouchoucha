{% extends 'dashboard/base.html.twig' %}

{% block title %}Edit Utilisateur{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1 class="h3 mb-4 text-gray-800">Edit Utilisateur</h1>

        <div class="card shadow mb-4">
            <div class="card-header py-3 d-flex justify-content-between align-items-center">
                <h6 class="m-0 font-weight-bold text-primary">Edit User</h6>
                <a href="{{ path('app_user_index') }}" class="btn btn-secondary btn-sm">
                    <i class="fas fa-arrow-left"></i> Back to list
                </a>
            </div>
            <div class="card-body">
                {{ include('dashboard/utilisateur/_form.html.twig', {'button_label': 'Update'}) }}

                <div class="mt-4">
                    {{ include('dashboard/utilisateur/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}