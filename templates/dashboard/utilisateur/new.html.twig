<!DOCTYPE html>

<title>New Utilisateur</title>

{% block body %}
    <h1>Create new Utilisateur</h1>

    {{ include('dashboard/utilisateur/_form.html.twig') }}

    <a href="{{ path('app_user_index') }}">back to list</a>
{% endblock %}
